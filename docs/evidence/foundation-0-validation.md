# Foundation 0 验证记录

日期：2026-08-28

## 自动化结果

- 后端格式与静态检查：通过。
- 后端测试：9 项通过。
- 前端测试：2 项通过，包含不安全 JSON 能力拒绝测试。
- Vue 类型检查：通过。
- 前端生产构建：通过。
- 生产依赖审计：未发现已知漏洞（使用 npm 官方审计端点）。
- 仓库敏感变量模式扫描：未发现硬编码 API Key、Secret、Password 或 Token。

## 浏览器验收

- 管理页面采用无顶部栏工作台，左侧导航在展开和收起状态均可用。
- 左侧菜单可收起、可再次展开，并将偏好保存在本机。
- 配件列表与新增页为独立路由；详情页使用 `/components/:id`。
- 模拟 BLE 小灯可通过安全能力入口执行打开命令，并显示模拟执行反馈。
- AI 示例可生成“开灯 → 等待 3 秒 → 关灯”的可编辑积木。
- 积木工作室覆盖事件、连接、传感器、执行器、流程、逻辑、数据日志和安全分类；工作区 JSON 可带项目与硬件元数据导入导出。
- 系统知识库采用分类文件库、搜索筛选、索引状态和独立详情；人工补充只以 `staged` 状态暂存，物理控制安全策略标记为不可自行优化。
- 最终新开浏览器页面无 console error 或 warning。

## 已知限制

- 当前是真实硬件开启前的模拟模式；尚未执行 BLE 扫描或 GATT 写入。
- 默认 AI 是完全本地的教学示例器；配置 Ollama 后可切换本地模型。
- Blockly 与 Element Plus 仍形成较大的按路由加载包，生产构建会给出体积警告，但不影响功能。
- Python 测试依赖中有一条 Starlette TestClient/httpx 弃用提示，来自上游依赖。
- 3D 工作台仅完成需求与架构存档，当前版本没有实现。
